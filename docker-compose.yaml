version: '3'
services:
  dns.service:
    restart: always
    container_name: sftp
    privileged: true
    image: "sftp:latest"
    build: .
    volumes:
      - /c/Users/SUPRASUMO/Documents/git/sftp:/go/src/sftp
      - /c/data/users:/data/users
      - /c/data/public:/data/public
      - /c/data/cache:/data/users_cache
    ports:
      - "24:22"
      - "5000:8081"